feat: distance-based delivery fee (1,500 so'm/km via Haversine)

- Replace flat 15,000 so'm delivery fee with distance-based calculation
- Haversine formula measures straight-line distance from store origin
  (Tashkent center 41.2995, 69.2401) to customer's saved location
- Rate: 1,500 so'm per km, minimum 3,000 so'm
- Free delivery threshold retained: orders >= 100,000 so'm stay free
- Fallback to 15,000 so'm when customer has no saved location
- Cart summary shows "X.X km × 1 500 so'm" subtitle under delivery row
- useLocation() hook wired into CartScreen for live coordinate access
- No backend schema changes needed: finalTotal already includes delivery
